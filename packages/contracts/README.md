# @assessflow/contracts

Shared, versioned domain contracts for the AssessFlow web and API packages.

The package intentionally contains stable codes and validation schemas, not database or UI logic. The API remains authoritative for transitions and permissions.
