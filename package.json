{
  "name": "assessflow",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "dev": "pnpm --parallel --filter @assessflow/api --filter @assessflow/web --filter @assessflow/worker dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm --filter @assessflow/contracts build && pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "format:check": "prettier --check package.json README.md \"apps/**/*.{ts,tsx,css,json,html}\" packages/contracts/package.json packages/contracts/src packages/database/package.json packages/database/prisma/seed.ts packages/database/scripts infra/bicep/README.md \".github/**/*.yml\"",
    "validate": "pnpm typecheck && pnpm test && pnpm build"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
